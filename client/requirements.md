## Packages
recharts | For visualizing trade volume trends
framer-motion | For slick animations on key triggers and UI elements

## Notes
Binance WebSocket: wss://stream.binance.com:9443/ws/<symbol>@aggTrade
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  mono: ["var(--font-mono)"],
  body: ["var(--font-body)"],
}
